:root {
  --cream: #f7f1e8;
  --ink: #1b1b1b;
  --muted: rgba(27, 27, 27, 0.62);

  /* Theme (toggled by the moving flowers) */
  --accent: #ff7a30; /* orange */
  --accent2: #2057ff; /* blue */

  --panelBg: rgba(255, 255, 255, 0.92);
  --panelLine: rgba(0, 0, 0, 0.12);
  --shadow: 0 14px 50px rgba(0, 0, 0, 0.14);
  --radius: 18px;

  --blue: #2057ff;
  --red: #c81c1c;
}

* {
  box-sizing: border-box;
}

html,
body {
  height: 100%;
}

body {
  margin: 0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial,
    "Noto Sans";
  color: var(--ink);
  background: var(--cream);
  overflow: hidden; /* landing view: no scrolling */
}

/* Background canvas */
#bg {
  position: fixed;
  inset: 0;
  width: 100vw;
  height: 100vh;
  z-index: 0;
}

.app {
  position: relative;
  z-index: 1;
  height: 100vh;
  display: grid;
  grid-template-columns: 1fr;
  transition: grid-template-columns 220ms ease;
}

.app.is-panel-open {
  grid-template-columns: 1fr 1fr;
}

.main {
  height: 100vh;
  display: grid;
  place-items: center;
  padding: 22px;
}

.center {
  width: min(900px, 92vw);
  text-align: center;
}

.hallucinate {
  appearance: none;
  background: transparent;
  border: none;
  cursor: pointer;
  display: inline-grid;
  justify-items: center;
  gap: 10px;
  padding: 8px 10px;
}

.hallucinate:focus-visible {
  outline: 3px solid rgba(0, 0, 0, 0.45);
  outline-offset: 6px;
  border-radius: 12px;
}

.hallucinate__reload {
  height: 28px;
  width: 28px;
  display: grid;
  place-items: center;
  border-radius: 99px;
  border: 1px solid rgba(0, 0, 0, 0.14);
  background: rgba(255, 255, 255, 0.7);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  opacity: 0;
  transform: translateY(6px);
  transition: opacity 160ms ease, transform 160ms ease;
}

.hallucinate.has-reload .hallucinate__reload {
  opacity: 1;
  transform: translateY(0);
}

.hallucinate__word {
  font-weight: 900;
  letter-spacing: -0.03em;
  line-height: 0.95;
  font-size: clamp(46px, 8vw, 110px);
  text-transform: uppercase;
}

.hint {
  margin-top: 10px;
  color: var(--muted);
  font-size: 13px;
}

.reports {
  margin: 18px auto 0;
  display: grid;
  gap: 12px;
  justify-items: center;
}

.report {
  width: min(720px, 92vw);
  text-align: left;
  background: rgba(255, 255, 255, 0.9);
  border: 1px solid rgba(0, 0, 0, 0.12);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 14px 16px;
}

.report__title {
  margin: 0;
  font-size: 18px;
  letter-spacing: -0.01em;
}

.report__meta {
  margin: 6px 0 0;
  color: var(--muted);
  font-size: 12px;
}

.report__body {
  margin: 10px 0 0;
}

/* Right arrow toggle */
.panelToggle {
  position: fixed;
  right: 16px;
  top: 50%;
  transform: translateY(-50%);
  z-index: 3;
  border: 1px solid rgba(0, 0, 0, 0.14);
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(10px);
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.14);
  border-radius: 999px;
  width: 54px;
  height: 54px;
  cursor: pointer;
  display: grid;
  place-items: center;
}

.panelToggle:focus-visible {
  outline: 3px solid rgba(0, 0, 0, 0.45);
  outline-offset: 4px;
}

.panelToggle__arrow {
  font-size: 20px;
  font-weight: 900;
  color: var(--ink);
}

/* Panel */
.panel {
  height: 100vh;
  border-left: 1px solid var(--panelLine);
  background: var(--panelBg);
  backdrop-filter: blur(12px);
  box-shadow: -10px 0 40px rgba(0, 0, 0, 0.12);
  overflow: auto;
}

.panel__inner {
  padding: 18px 18px 70px;
  max-width: 720px;
}

.panel h2 {
  margin: 0;
  font-size: 16px;
  text-transform: uppercase;
  letter-spacing: 0.09em;
}

.panel p {
  margin: 10px 0 0;
  color: var(--muted);
  font-size: 13px;
}

.panelSection {
  margin-top: 16px;
  border: 1px solid rgba(0, 0, 0, 0.12);
  border-radius: var(--radius);
  background: rgba(255, 255, 255, 0.75);
  padding: 12px;
}

.panelSection h3 {
  margin: 0;
  font-size: 14px;
}

.panelSection h3.blue {
  color: var(--blue);
}

.panelSection h3.red {
  color: var(--red);
}

.panelSection ul {
  margin: 10px 0 0;
  padding-left: 18px;
}

.panelSection li {
  margin: 6px 0;
  font-size: 13px;
}

.template {
  margin-top: 10px;
  padding: 10px;
  border-radius: 12px;
  background: rgba(0, 0, 0, 0.04);
  border: 1px solid rgba(0, 0, 0, 0.1);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
    "Liberation Mono", "Courier New", monospace;
  font-size: 12px;
  white-space: pre-wrap;
}

.marked {
  margin-top: 10px;
  padding: 10px;
  border-radius: 12px;
  background: rgba(0, 0, 0, 0.02);
  border: 1px solid rgba(0, 0, 0, 0.1);
  font-size: 13px;
  line-height: 1.45;
}

.mark-blue {
  background: rgba(32, 87, 255, 0.14);
  color: #0e2cff;
  border-radius: 8px;
  padding: 0 6px;
}

.mark-red {
  background: rgba(200, 28, 28, 0.12);
  color: #b00000;
  border-radius: 8px;
  padding: 0 6px;
}

/* Theme flags for UI */
body[data-theme="orange"] {
  --accent: #ff7a30;
}

body[data-theme="blue"] {
  --accent: #2057ff;
}

/* Subtle accent underline */
.hallucinate__word {
  background-image: linear-gradient(
    to right,
    transparent,
    rgba(0, 0, 0, 0.02),
    transparent
  );
  border-bottom: 6px solid rgba(0, 0, 0, 0.08);
  padding-bottom: 6px;
}

@media (max-width: 980px) {
  .app.is-panel-open {
    grid-template-columns: 1fr;
  }

  .panel {
    position: fixed;
    right: 0;
    top: 0;
    width: min(520px, 92vw);
    z-index: 2;
  }
}
